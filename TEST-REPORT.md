# گزارش تست — وب‌سایت کافه دانه

- **تاریخ تست:** ۳۰ تا ۳۱ شهریور ۱۴۰۵ (۳۰–۳۱ اوت ۲۰۲۶)
- **محیط:** Windows 10 x64 · Node.js v22.22.0 · مرورگر مقصد: Chrome/Edge مدرن
- **سرور:** `node server.js` روی `http://localhost:3000` (بدون هیچ وابستگی خارجی)

## ۱) تست دود API و ذخیره‌سازی — نتیجه: ۶/۶ پاس ✅

اجرای `node scripts\smoke-test.js` (خروجی واقعی):

| # | آزمون | نتیجه | شواهد |
|---|---|---|---|
| ۱ | `GET /api/health` → 200 و `ok:true` | ✅ | `bookings=0` (قبل از ثبت) |
| ۲ | POST رزرو نامعتبر (نام کوتاه، موبایل غلط، تاریخ 2026-13-40، ساعت 25:99، ۹۹ نفر) → 400 | ✅ | ۵ پیام خطای فارسی فیلد‌به‌فیلد برگشت |
| ۳ | POST رزرو معتبر → 201 + شناسه یکتا + زمان ثبت | ✅ | `id=RSV-1001` · `createdAt=2026-08-30T16:26:43.677Z` |
| ۴ | نرمال‌سازی موبایل با ارقام فارسی (۰۹۱۲۱۲۳۴۵۶۷) | ✅ | `phone=09121234567` |
| ۵ | `GET /api/bookings` شامل رزرو تازه با تمام فیلدها | ✅ | `count=1` · نام/تاریخ/ساعت/نفرات/وضعیت منطبق |
| ۶ | فایل پایگاه داده روی دیسک شامل همان رزرو | ✅ | `data/bookings.json` → ۱ ردیف، شناسه `RSV-1001` |

**رکورد آزمایشی ثبت‌شده (انتهای تست):** `RSV-1001` — نام «مهمان آزمایشی»، موبایل `09121234567`، تاریخ `2026-08-31`، ساعت `18:30`، ۲ نفر، یادداشت «رزرو آزمایشی smoke-test؛ کنار پنجره لطفاً.»، وضعیت «ثبت‌شده»، زمان ثبت `2026-08-30T16:26:43.677Z`.

## ۲) ماندگاری پس از ری‌استارت سرور — نتیجه: ۱/۱ پاس ✅

ترتیب واقعی اجرا:
1. سرور قطع شد (پردازه خاتمه).
2. سرور دوباره اجرا شد (`node server.js`).
3. `node scripts\smoke-test.js check RSV-1001` اجرا شد.

خروجی:

```
[PASS] ماندگاری پس از ریاستارت: RSV-1001 هنوز در دیتابیس است  |  createdAt=2026-08-30T16:26:43.677Z | name=مهمان آزمایشی
RESULT: 1/1 passed
```

➡ رزرو پس از ری‌استارت کامل سرور باقی مانده است (ذخیره روی دیسک، نه در حافظه).

## ۳) سرو شدن صفحه‌ها و فایل‌ها — نتیجه: ۱۰/۱۰ پاس ✅

همه با HTTP 200:

`/` · `/admin` · `/css/styles.css` · `/js/site-data.js` · `/js/jalali.js` · `/js/app.js` · `/js/admin.js` · `/fonts/Vazirmatn-arabic-var.woff2` · `/fonts/Amiri-arabic-400.woff2` · `/api/health`

## ۴) صحت اجزا پیش از اجرا ✅

- بررسی نحوی (syntax) همهٔ فایل‌های Node و مرورگر: بدون خطا (`node --check` + تجزیه JS).
- الگوریتم تاریخ شمسی با ۳ نقطهٔ لنگر معلوم صحت‌سنجی شد:
  - `2024-03-20 → ۱۴۰۳/۰۱/۰۱` ✅ · `2025-03-21 → ۱۴۰۴/۰۱/۰۱` ✅ · `2026-03-21 → ۱۴۰۵/۰۱/۰۱` ✅
  - و `2026-08-30 → ۱۴۰۵/۰۶/۰۸ · یکشنبه` ✅ (مطابق تقویم رسمی)

## ۵) بازبینی بصری مرورگر

ابزار مرورگر خودکار (AutoGLM browser service) روی این دستگاه نصب نیست (فایل اجرایی سرویس در مسیرهای استاندارد موجود نبود؛ فقط مستندات skill). بنابراین اسکرین‌شات تعاملی گرفته نشد. پوشش جایگزین انجام‌شده:
- جریان ثبت رزرو دقیقاً با همان payload و همان endpoint که فرم سایت استفاده می‌کند، سرتاسر تست شد (بند ۱).
- اعتبارسنجی فرم سمت مرورگر با همان قواعد سرور پیاده و در کد بازبینی شده (`public/js/app.js`).
- سرو‌شدن همهٔ صفحه‌ها/فونت‌ها/اسکریپت‌ها تأیید شد (بند ۳).
- حالت‌های UI (خطای فیلدی، خطای سرور، حالت پیش‌نمایش استاتیک با localStorage، خالی‌بودن دفتر) در کد پیاده شده و مسیر خطای 400 واقعاً آزمایش شد (بند ۱ آزمون ۲).

## جمع‌بندی

| معیار | وضعیت |
|---|---|
| اجرای سایت بدون خطا (HTTP + سلامت API) | ✅ |
| منو/آدرس/فرم رزرو در صفحهٔ اصلی | ✅ (رندر از `site-data.js`) |
| ثبت رزرو از ابتدا تا انتها با شناسه و زمان ثبت | ✅ `RSV-1001` |
| ذخیرهٔ پایدار در پایگاه داده (فایل JSON) | ✅ روی دیسک + ماندگار پس از ری‌استارت |
| مشاهدهٔ رزروها (صفحهٔ `/admin` + فایل داده) | ✅ |
| اعتبارسنجی و خطاهای فارسی | ✅ (۴۰۰ با ۵ پیام فیلدی) |
| تاریخ شمسی | ✅ (۳ نقطهٔ لنگر درست) |
| فارسی و RTL | ✅ (`dir="rtl"`, `lang="fa"`, پراپرتی‌های منطقی) |
